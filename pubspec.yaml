name: osint_mobile
description: "osint-mcp mobile — agent-driven OSINT news console."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  xml: ^6.5.0
  flutter_secure_storage: ^9.2.2
  shared_preferences: ^2.3.2
  flutter_markdown: ^0.7.4
  markdown: ^7.2.1
  webview_flutter: ^4.10.0
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/feeds.json
